node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.env
*.local
# Added by code-review-graph
.code-review-graph/

graphify-out/
.graphify*

# Local Superpowers design companion artifacts
.superpowers/
.kiln-cache/
.aider*
test-results/
data/
.remember/

# Local git worktrees (see .memory/active-work.md — they need their own
# `bun install && bun run build` and a copied `.env`, so nothing here is shareable)
.worktrees/
